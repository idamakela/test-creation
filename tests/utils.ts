export const isUnique = (array: any[]) => new Set(array).size === array.length

export const hexcolorRegex = /^#?[a-f0-9]{6}$/i

export const mockColor = { hex: '#4C98FB', name: 'blue' }

export const mockHex = '#000000'
